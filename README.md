XO — Q-Learning Tic Tac Toe
A simple web project to play Tic Tac Toe (XO) against an AI agent trained with Q-Learning.
The agent has been pre-trained through self-play for tens of thousands of games, making it a tough opponent.

🔹 Components
Backend:

Python + Flask

Q-Learning algorithm for state-action value learning

Simple API endpoints:

/state → Get the current game state

/move → Submit a player move

/reset → Reset the game

Frontend:

HTML / CSS for modern dark-themed UI

JavaScript to handle API calls and render the board

Option to choose whether to play as X or O

